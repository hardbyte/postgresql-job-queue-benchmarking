[project]
name = "postgresql-job-queue-benchmarking-absurd"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "absurd-sdk==0.3.0",
    "psycopg[binary]==3.3.3",
]

[tool.uv]
package = false
